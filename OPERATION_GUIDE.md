# 足球预测系统 — 实战操作手册
> 系统稳定后的标准操作流程。每次会话遵循本手册。

---

## ★ 完整闭环流程（每轮比赛的标准动作，先读这里）

```
拉取(带时间戳) → 分析 → 落账冻结(赛前5min截止) → 临场风控 → 赛后结算(独立) → 无泄漏复盘 → 周期校准
```

### 第1步 赛前数据拉取（每日定时，快照必须带时间戳落盘）
```bash
python prediction_v2/fetch_live_odds.py --league 联赛   # 赔率快照 -> output/odds_snapshots/snapshots.csv
python prediction_v2/_batch_match_info.py --with-bsd     # BSD: 伤停/共识赔率/积分榜/预期进球
```
开赛前 30 分钟补一次临场盘（见〇节）。

### 第2步 分析（开赛前）
```bash
python prediction_v2/scan_upcoming.py                    # λ/方向/EV/星级 + BSD交叉验证
```
输出必须标注：数据源、快照时间、伤停情报；无情报/缺盘必须标注，禁止静默填0。

### 第3步 落账冻结（赛前5分钟截止，之后任何人不得改预测）
```bash
python prediction_v2/bet_ledger.py --scan                # best_bet落账(去重)
```
账本每行必填：odds=**落账那一刻拉到的价**、kickoff、snap_age_h、data_src、veto、risk_tags。

### 第4步 临场风控（T-30min）
```bash
python live_odds_check.py                                # 初盘 vs 临场，区分方向/陷阱
```
触发 veto（分歧>20pp / 盘口过期 / 临场反向）→ 标记但不删除；**被否决的注也入账**，供复盘校准否决信号。

### 第5步 赛后结算（独立于预测，只回填结果）
```bash
python prediction_v2/settle_live.py "主队 vs 客队 2-1"
python prediction_v2/bet_ledger.py --settle 结算文件
```
赛果三源交叉（结算JSON / ESPN CSV / BSD events）；绝不回头修改赛前预测。

### 第6步 无泄漏复盘（每周 / 每50注）
```bash
python prediction_v2/ledger_backtest.py                  # 只用赛前冻结，剔除snap<0，分 全量/实单/被否决/市场/联赛
python prediction_v2/ledger_vs_data.py                   # 逐注对"当时快照价"，区分 数据问题 vs 模型问题
```
判定口径：命中率 = (win + 0.5×half)/已决；平注ROI = 每注1单位。**连续50注ROI<0 → 停用该方向**。

### 第7步 周期校准（每100注）
1. 用账本做校准集：Platt / 保序回归 校准模型概率（当前主因=概率虚高，非快照过期）
2. 重设 EV 阈值、停用 ROI<0 的联赛/方向
3. 校准后 `python regression_test.py` 全绿，再进下一轮

### 防泄漏铁律
1. 预测只在开赛前生成，开赛后不改
2. 落账赔率=落账时刻拉取值（带快照时间戳）
3. `snap_age_h < 0`（赛后才拉盘）一律作废，不纳入复盘
4. 结算独立，只填结果
5. 复盘只用冻结数据，禁止用结果反推选注

---

## 〇、临场盘口对比（区分方向/陷阱，最关键）

**工具：`python live_odds_check.py`**

### 用法
1. 分析时给初盘（系统已存）
2. **开赛前30分钟**运行工具，填临场最新数字
3. 自动对比 → 判断方向/陷阱

### 判断依据（J1/K2临场规律）
| 变盘 | 判断 |
|------|------|
| 升盘+同步降水 | ✅ 可信方向 |
| 升盘+升水 | ❌ 诱上陷阱 |
| 深盘升盘 | ⚠️ 诱热难穿盘 |
| 急速跳水降水 | ❌ 大热必死 |
| 无利空退盘+升水 | ✅ 阻上洗筹 |
| 降盘+降水(有伤缺) | ❌ 真实看衰 |

**经验**：同步升盘降水可信；独自升盘升水/急速跳水=陷阱。市场>历史数据（金浦教训）。

---

## 一、赛前分析流程（每次比赛）

### Step 1: 数据输入
1. 用户发**盘口数据** → 数据提取器自动解析（主客区分+盘口+细分）
2. 用户按标准格式发**情报**：
   ```
   1. 主队近10场（主客场战绩/进失球/射门射正）
   2. 客队近10场（同上）
   3. 历史交锋（比分/日期）
   4. 伤病停赛（门将/防线/核心）
   5. 战意（排名/升级保级形势）
   ```

### Step 2: 完整30步SOP（一步不跳）
```
数据提取器 → Agent风险信号 → 模型计算(λ/比分/概率) → 新引擎v2交叉验证 → 冷门引擎 → Agent战术 → EV打星
```
**输出规范（必须展示完整链条）：**
1. Step 0.5 数据提取器（主客+盘口+情报分离）
2. Step 7.5 Agent风险信号（各信号值+方向）
3. Step 8 模型计算（λ+TOP比分+大小球/让球概率）
4. Step 8.5 新引擎(prediction_v2)交叉验证（v2候选表：边际/赔率/EV + 推荐标记；9大联赛走数据集特征，小联赛走情报+联赛真值）
5. Step 30 Agent战术分析（DeepSeek完整输出）
6. Step 31 EV打星（候选表+best_bet+final_best_bet，v2推荐为准）

### Step 3: 联赛真值校准（关键）
查 `strategy_data/*_odds_zones.json`：
- **K2** → `k2_odds_zones.json`（区间1-5真实胜平负+亚盘赢盘率）
- **中乙** → `cl2_odds_zones.json`
- **核心应用**：用区间真值判定方向，不反推λ
  - 深盘（主让≥1）→ 受让方安全，防走盘
  - 高赔主场（主赔>2.8）→ 客队方向
  - 中庸盘（1.76-2.25）→ 平局价值最高
  - 平局赔率3.2-3.35 → 重点防平

---

## 二、投注纪律

| 规则 | 说明 |
|------|------|
| **只用best_bet** | 串关必须用最高胜率腿，禁止改选其他腿 |
| **小注多场** | 单场≤5%本金，串关≤2%本金 |
| **方向集中** | 同轮比赛优先同方向（K2多客队/受让方向）|
| **高赔慎追** | 高倍串关（40倍+）是博冷，1-2套小注即可 |
| **互斥理解** | 方向相反的串关不可能同时全对，别当对冲买 |

---

## 三、赛后结算流程

1. 用户发**6场比分** → 系统自动结算（每腿赢/输/走水）
2. **v2 逐注账本结算（新）**：每场分析推荐已自动入账 `prediction_v2/output/live_bets.jsonl`，赛后执行：
   ```bash
   python prediction_v2/settle_live.py "金浦 vs 忠南 1-1" "温州 vs 贵州 2-0"
   ```
   输出每注赢/输/走水 + 平注口径 P&L（2.5 为半球盘无走水；整数盘如 2.0 走水）
3. 记入胜率统计 `strategy_data/winrate_stats.json`
4. 对比 best_bet vs 实际 → 验证方法是否应验
5. **重点验证 v2 小球纪律**：小球2.5（边际≥5%）每 50 注结算一次 ROI，连续 2 个周期为负即停用
6. 重点验证：
   - K2区间真值方法是否应验（深盘受让/高赔客场/平局）
   - 中乙区间真值方法是否应验
7. 更新 SESSION_STATE.md

---

## 四、验证闭环（稳定核心）

```
每轮比赛 → 结算 → 胜率更新 → 回归测试确认 → 下次沿用
```

### 长期策略
1. **数据积累**：每场结算入账，攒到50场看真实胜率
2. **只信数据**：某类预测连续20场胜率<50% → 停用该方向
3. **联赛特化**：K2用K2真值、中乙用中乙真值、其他联赛按需建基准
4. **复盘优先**：每场错单找原因（方向错/数据错/模型错）→ 改进

---

## 五、防退化铁律

1. **每次改代码/提示词/SOP** → 必须 `python regression_test.py` 全绿
2. **遇到新数据格式** → 查 `DATA_FORMATS.md` → 清单外走新格式流程
3. **回归73+项** = 历史正确结论的质检闸门

---

## 六、风险提示

- **6串1组合胜率10-16%**（3套打法约20-35%至少中1套）
- **长期期望接近打平**：靠中大奖覆盖小亏，小注娱乐
- **系统强项**：让球盘方向（回测64%）、K2/中乙区间真值、冷门引擎
- **系统弱项**：精确比分（~15%）、胜平负（~50%和市场打平）、单场大小球

---

## 七、当前状态速查

- 最佳模型：XGBoost+Platt 53.8%
- 回测验证：让球盘64%>胜平负50.7%
- K2真值：深盘受让+高赔客场（已验证中乙5场80%命中）
- 回归测试：900+项（含2026-08-26新增"防泄漏口径"回归）
- **实盘真值（2026-08-26审计）**：合规口径762注 ROI −1.41%；⚠️已废弃"Pinnacle +240%"历史回测声称（不可复现，见AGENTS.md）
- snap<0（赛后拉盘）行：结算脚本自动标"无效(snap<0赛后盘)"，不进任何统计（防泄漏铁律已代码化）
- 待结算：以 `SESSION_STATE.md` 最新记录为准
