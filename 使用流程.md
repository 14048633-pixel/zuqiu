# 足球预测系统 — 使用流程（完整版）

> 目标机部署完成后，按本手册进行日常使用。详细 SOP 见 `SOP_30STEP.md`，实战纪律见 `OPERATION_GUIDE.md`。

---

## 一、部署与首次启动（一次性）
1. 解压迁移包到目标目录（如 `D:\football_system`）
2. 安装 Python 3.10+（官网下载，勾选 **Add Python to PATH**）
3. **推荐：双击 `一键部署.bat`**（自动检查 Python → 跑部署验证 → 打开使用流程）
4. 或在解压目录手动运行一键部署验证：
   ```
   python setup_verify.py
   ```
   看到 `🎉 全部通过`（含回归测试 106 项全绿）即部署成功。
4. 检查 API 密钥：打开 `.env`，确认 `DEEPSEEK_API_KEY` / `DOUBAO_API_KEY` / `ODDS_API_KEY` 等已填写（换账号就替换成自己的 key）
5. 验证盘口 API（可选）：
   ```
   cd prediction_v2
   python fetch_live_odds.py --test
   ```

## 二、日常赛前分析（每次比赛的标准动作）
完整流程是 30 步 SOP（`SOP_30STEP.md`），日常最小闭环：

1. **收集数据**（禁止编造）
   - 盘口：初盘 + 临场（主胜/平/客胜、让球、大小球）
   - 情报：主客队近 10 场、历史交锋、伤停、战意
2. **联赛真值校准**（关键，方向判定用，不反推 λ）
   - 查 `strategy_data/*_odds_zones.json`：J1→`j1_odds_zones.json`、K2→`k2_odds_zones.json`、中乙→`cl2_odds_zones.json`、中超→`csl_odds_zones.json`
3. **跑模型分析**
   ```
   python auto_sop.py 比赛数据.json
   ```
   （或让 AI 按 30 步 SOP 输出完整链条：数据提取 → Agent 风险信号 → λ/比分/概率 → v2 交叉验证 → 冷门引擎 → Agent 战术 → EV 打星 → best_bet）
4. **临场盘口对比**：开赛前 30 分钟
   ```
   python live_odds_check.py
   ```
   同步升盘降水=可信方向；独自升盘升水/急速跳水=诱盘陷阱。
5. **落盘**：分析记录保存到 `analysis_records/`，推荐入账 `prediction_v2/output/live_bets.jsonl`

## 三、盘口数据抓取（the-odds-api）
```
cd prediction_v2
python fetch_live_odds.py --test           # 验证 key + 配额（只 1 个请求）
python fetch_live_odds.py --league 英超    # 只抓单联赛
python fetch_live_odds.py                  # 抓全部 9 联赛
```
输出到 `prediction_v2/output/odds_snapshots/`。注意：免费 key 不支持 htft/上半场等市场；剩余配额低于下限会自动停止（`--min-remaining` 可调）。

## 四、投注纪律（铁律，禁止违反）
- **只用 best_bet**：EV≥0 且赔率≥1.60 前提下选胜率最高腿
- **串关必须用 best_bet 最高胜率腿**，禁止手动改选其他腿
- 单场 ≤5% 本金；串关 ≤2% 本金
- 方向互斥的腿不要串在一起
- 高赔博冷（40 倍+）只 1-2 套小注

## 五、赛后结算
```
python prediction_v2/settle_live.py "金浦 vs 忠南 1-1" "温州 vs 贵州 2-0"
```
- 自动对账 v2 账本（`prediction_v2/output/live_bets.jsonl`），输出每注赢/输/走水
- 更新 `strategy_data/winrate_stats.json` 胜率统计
- 小球 2.5 每 50 注结算一次 ROI，连续 2 个周期为负 → 停用该方向
- 每场复盘：方向错/数据错/模型错 → 改进

## 六、质检与防退化（改完必跑）
- **改任何代码/提示词/SOP 后必须**：
  ```
  python regression_test.py
  ```
  全绿（106 项）才能继续；变红说明破坏了历史正确结论。
- 遇到新数据格式：先查 `strategy_data/DATA_FORMATS.md`，清单外的格式必须停下确认，禁止猜格式或静默填 0。

## 七、数据收集与校准（选做）
- 深盘大球策略数据：`python collect_strategy_data.py`
- 联赛基准 API 校验示例：`python strategy_data/fetch_league_baseline_check.py`
- 更新 `strategy_data/*_odds_zones.json` 后必须重跑回归测试

## 八、冷门引擎（独立迁移包）
```
cd upset_engine_migration
python validate.py
```
看到 `🎉 全部验证通过` 即引擎可用。

## 九、常用命令速查表
| 目的 | 命令 |
|------|------|
| 一键部署验证 | `python setup_verify.py` |
| 回归质检 | `python regression_test.py` |
| 自动分析 | `python auto_sop.py 比赛数据.json` |
| 抓实时盘口 | `python prediction_v2/fetch_live_odds.py --league 英超` |
| 临场盘口对比 | `python live_odds_check.py` |
| 赛后结算 | `python prediction_v2/settle_live.py "A vs B 1-1"` |
| 冷门引擎验证 | `cd upset_engine_migration && python validate.py` |

## 十、目录结构速查
```
strategy_data/      联赛真值/盘口快照/结算/复盘（J1/K2/中乙/中超/丹麦杯/欧战）
analysis_records/   历史分析记录（每场存档）
prediction_v2/      the-odds-api 抓取 + v2 引擎 + 逐注账本
src/                核心模块（模型/规则/特征/策略/Agent/冷门引擎）
models/             预训练模型
data/               训练数据（raw/processed/xg/db/odds_history）
upset_engine_migration/  冷门引擎独立包
videos/ images/     素材（非运行必需）
archive/            旧版脚本归档（仅供参考，勿用于生产）
```
