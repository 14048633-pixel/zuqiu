# 底层数据模块问题与优化方案（完整版 v2）

> 范围：泊松 λ 公式 / EV 去水 / LLM 权责边界 / 风控 / 串关 / 仓位 / 回测 / 运维 / 合规。
> 状态：方案评审稿（未实施代码改动）。落地原则：改完必跑 `python regression_test.py` 全绿，新逻辑写回归断言。

---

## 〇、三大核心疑问落地设计

### 1. 缺失数据分级处理规则

**分级标准（按字段重要性打分，0-100 完整性分）：**

| 级别 | 判定条件 | 处理动作 |
|---|---|---|
| 一级缺失 | 次要字段缺失（10 场少 2 场、无历史交锋、无阵型） | 保留样本；特征用联赛均值/历史均值填充；日志标 `missing_tag`；统计单独分组 |
| 二级缺失 | 核心字段缺失（无伤病、无攻防数据、无基础盘口） | 强制阻断分析；缺失字段写入告警日志；走降级替代（问用户 / 联赛基准+市场盘口定性） |
| 三级缺失 | 无盘口且无任何攻防数据 | 拒绝分析，不产出推荐 |

**落地**：`data_completeness.py` 检查器，分析前输出完整性报告 `missing_report`（字段名 / 级别 / 填充值 / 是否阻断）。

### 2. 异常赛事清洗逻辑

**异常类型与统一标签：**

| 类型 | 标签 | 处理 |
|---|---|---|
| 友谊赛 | `exclude_friendly` | 剔除出训练与胜率统计 |
| U21/U19/青年队 | `exclude_youth` | 剔除 |
| 延期/腰斩 | `exclude_postponed` | 标记并剔除，不参与结算 |
| 判改比分 | `flag_amended` | 保留但标记，结算按官方判改 |
| 中立场 | `flag_neutral_venue` | 保留，主场优势系数归 1.0 |
| 两回合杯赛 | `split_two_leg` | 拆分主客场，分别修正 λ 基准 |

**落地**：`fetch_live_odds.py` 赛事过滤黑名单 + 标签字段，`settle_live.py` 按标签结算。

### 3. 赔率源单一依赖处理

| 层级 | 机制 |
|---|---|
| 主源 | the-odds-api（ODDS_KEY_1） |
| 备用源 | 多组 ODDS_KEY（.env 数组），请求失败/限流/超时自动切换 |
| 交叉校验 | 同场次多家赔率取 Pinnacle 为主，分歧 >5% 标记 `odds_divergence` |
| 降级 | 全部源失败 → 复用最近一次快照，标 `data_stale`，临场对比禁用 |

**落地**：`.env` 支持 `ODDS_KEY_1..N`；`src/live_odds.py` 增加多 key 轮询与健康检查。

---

## 一、泊松 λ 核心计算公式缺失（致命漏洞）

### 现存问题
1. 全文仅一句话“λ = 真实攻防数据 + 特征修正 + Agent风险信号修正”，无量化公式：主客场系数、双线疲劳、伤停减分、交锋偏移、联赛攻防基准无数值定义；
2. Agent 0-1 信号如何修正 λ 无权重，LLM 定性描述易大幅干扰数值，破坏“数值由模型产出”铁律；
3. 未区分主客双 λ（λH / λA），无法严谨算胜平负、大小球、让球；
4. 无 λ 上限截断，强强/强弱对战易产出不合理高 λ，比分概率失真。

### 修改方案（写入 SOP Step 8）
- 标准化公式：
  `λ_base,H = Attack_avg,H × Defend_avg,A × LeagueFactor`
  `λ_H = λ_base,H × HomeCoef × FatigueCoef × InjuryCoef + RiskSignal × AdjustWeight`
  `λ_A = λ_base,A × AwayCoef × FatigueCoef × InjuryCoef − RiskSignal × AdjustWeight`
- Agent 风险信号权重上限：**最大 ±10% λ 修正幅度**，LLM 仅输出定性文字；
- λ 上下限：**λ ∈ [0.2, 4.5]**，超区间自动截断并告警。

## 二、赔率偏差量化、EV 缺少去水（逻辑硬伤）

### 现存问题
1. Step4 R119 仅量化原始赔率偏差，未归一去水，EV 系统性高估；
2. Step31 `EV=概率×赔率−1` 仅适用无抽水公平赔率，真实盘口必须先去水；
3. 未区分独赢/大小球/让球抽水率差异。

### 修改方案
- 新增全局去水模块，SOP 前置抽水率计算：`Overround = 1/WinOdds + 1/DrawOdds + 1/LoseOdds`；
- 公平概率 = 原始模型概率 / Overround 校正系数；
- 修正 EV 标准：`EV = FairProb × RawOdds − 1`，所有打星、best_bet 基于去水后公平 EV；
- 分市场存储各联赛平均抽水率，大小球/让球单独校准。

## 三、LLM 权责边界不清晰

### 现存问题
- “数值必须模型计算”与 Step30 LLM 战术分析无强隔离，prompt 溢出可篡改 λ/EV/胜率；
- 冷门引擎与 LLM 耦合，未明确冷门等级仅由独立引擎输出。

### 修改方案
- 代码层硬隔离：LLM 接口只允许输出文本注释，概率/λ/EV/冷门分值由 Python 规则/泊松输出；增加校验脚本，LLM 输出数字参数直接阻断并告警；
- SOP 拆分：Step29.5 冷门等级 = 独立引擎纯数值输出；Step30 LLM 仅辅助解读。

## 四、风控与让球验证逻辑单一

### 现存问题
- Step7 仅“让平集中度≤50%”单一指标，未覆盖赔率分歧、历史同盘口亏损率、联赛波动、连亏风控，无动态风险分级。

### 修改方案
- 多维风控信号：多机构赔率分歧指数（胜赔标准差）；该盘口区间近 50 场历史 ROI；球队近期连续大/小球偏移；
- 多指标加权生成 0-1 风险分；**风险分 >0.6 直接降级 EV 星级，取消 best_bet 资格**。

## 五、best_bet / 串关 / 仓位风控模块缺陷

### 5.1 best_bet 过滤缓冲
- EV 安全阈值：**基础筛选 EV≥8%**，0~8% 为噪声区间仅观察不推荐；
- 叠加策略历史校验：对应联赛+盘口近 100 注 ROI<0 直接剔除；
- 赔率下限联赛可配置：高级联赛 1.6，次级联赛 1.4（存 odds_zones.json）。

### 5.2 串关统计漏洞
- 相关性矩阵：同联赛/同赛区加相关性衰减系数，串关联合概率 = 单场概率 × 相关修正因子；
- **仅允许 2 串 1，禁止 3 串 1 及更高；同联赛最多 1 场**；
- 仓位动态调整：整体 EV 越高、回撤越小，小幅提升仓位，上限仍 ≤2% 本金。

### 5.3 仓位静态化，无动态止损
- 单日总投注 ≤ 本金 15%；
- 连亏 3 注仓位减半；连亏 5 注当日停止推荐；
- 单策略最大回撤达本金 10% → 自动停用 ≥7 天；
- 单周期盈利超本金 20% → 提取 50% 利润锁定。

## 六、回测 / 回归 / 防退化短板

### 现存问题
- regression_test.py 仅防“改坏旧样本”，不解决过拟合、幸存者偏差、未来数据泄露；
- 无标准离线回测脚本，无夏普/最大回撤/盈亏比/分联赛 ROI/连亏分布；
- 无时间序列隔离约束。

### 修改方案
- 新增 `backtest_offline.py`：强制时间切片训练/验证/测试，禁止随机打乱；
- 回测报告强制输出：总 ROI、月度分联赛收益、最大回撤、胜率、盈亏比、平均 EV、最长连亏、样本总量；
- 过拟合检测：训练集 ROI − 测试集 ROI > 15% → 自动告警，策略不可上线。

### 复盘归因自动化
- `settle_live.py` 结算自动打标签：数据异常 / λ 偏差 / EV 噪声 / 突发情报 / 盘口诱盘 / 正常波动；
- 按月生成归因统计报表，自动定位失效模块。

## 七、工程与运维规范

1. 版本管理：SOP/模型脚本标记 vX.X，archive 按版本归档；
2. 定时清理：>90 天盘口快照压缩归档，>180 天自动删除；
3. 每日自动备份 strategy_data 核心配置；
4. `fetch_live_odds.py` 硬阻断：剩余配额低于阈值直接终止，不产出残缺盘口文件。

## 八、合规与风险隔离

- 文档首页、部署脚本、推荐输出头部增加固定免责声明；
- `live_bets.jsonl` 增加风险标签，运行强制提示随机性风险；
- 新增《合规使用规范.md》：仅限赛事数据研究，严禁大额赌博，禁止未成年人使用。

## 九、优先级整改清单

| 优先级 | 事项 |
|---|---|
| P0 立即 | 泊松 λ 完整公式；EV 去水逻辑；LLM 代码层硬隔离；串关相关性修正+场次限制 |
| P1 1周内 | 动态仓位止损止盈；离线时间序列回测+过拟合检测；数据缺失/异常赛事分级清洗 |
| P2 迭代期 | 赔率多源容灾；盘口异动时间权重量化；防时间穿越校验；自动复盘归因；磁盘清理备份 |
| P3 补充 | 合规免责文档；冷门引擎/多维风控公式细化；所有阈值系数统一至 json 配置 |

## 十、额外建议

- 模块解耦：auto_sop.py 拆分为 数据层 / 模型层 / 赔率分析层 / 风控层 / 输出层 5 子模块；
- 可视化监控面板：盘口变动曲线、历史 ROI 走势、每日信号数量；
- 仿真测试账户：新策略先仿真 300+ 场，实盘小资金试运行，不直接全量启用。